import Link from "next/link"

const Nav = () => {
    return (
        <nav className="flex items-center justify-around fixed top-0 left-0 bottom-0 m-3 bg-primary w-[300px] rounded-lg p-3">
            
        </nav>
    )
}

export default Nav