import "@/styles/globals.css"; // Import des styles globaux de l'application
import Nav from "@/app/components/Nav"; // Import du composant de navigation
import { TRPCReactProvider } from "@/trpc/react"; // Import du fournisseur de contexte TRPC pour les appels API
import Provider from "@/app/components/Provider"; // Import du fournisseur de contexte NextAuth
import type { ReactNode } from "react"; // Import du type ReactNode pour typer les props
import ReactQueryProvider from "./components/ReactQueryProvider"; // Import du fournisseur de contexte React Query

// Métadonnées de l'application pour le titre, la description et les icônes
export const metadata = {
  title: "Media Bouderga",
  description: "Generated by Abdel-samade Bouderga",
  icons: [{ rel: "icon", url: "/favicon.ico" }],
};

interface RootLayoutProps {
  children: ReactNode; // Déclaration du type des enfants du composant
}

// Composant de mise en page racine qui enveloppe l'application entière
export default function RootLayout({ children }: RootLayoutProps) {
  return (
    <html lang="en"> {/* Définit la langue du document HTML */}
      <body className="overflow-x-hidden"> {/* Application des styles CSS pour le corps */}
        <Provider> {/* Fournit le contexte NextAuth à l'application */}
          <ReactQueryProvider> {/* Fournit le contexte React Query à l'application */}
            <TRPCReactProvider> {/* Fournit le contexte TRPC à l'application */}
              <Nav /> {/* Affiche le composant de navigation */}
              {children} {/* Rend les composants enfants passés au composant */}
            </TRPCReactProvider>
          </ReactQueryProvider>
        </Provider>
      </body>
    </html>
  );
}
